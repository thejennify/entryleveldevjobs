import styled from 'styled-components';

export const Container = styled.main`
   margin: 0 2rem;
   z-index: 0;

`;
export const Title = styled.p`
    font-size: 20px;
    font-weight: 600;
    display: flex;
    align-items: center;
`;
